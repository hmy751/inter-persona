"use client";

import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    font: {
      gray: "#A8A69D",
    },
    gray: {
      100: "#EDF2F7",
    },
    mainBlue: "#D4E3FB",
    mainGray: "#F3F6F7",
  },
});
