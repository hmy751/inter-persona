export enum ChatContentStatusType {
  success = 'success',
  loading = 'loading',
  fail = 'fail',
  idle = 'idle',
}

export enum ChatContentSpeakerType {
  user = 'user',
  interviewer = 'interviewer',
}
